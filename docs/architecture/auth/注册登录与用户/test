好的，现在我给你一份**完整的 Apifox 测试方案**，按步骤从头到尾走一遍就能验证所有改造点。

---

## 前置准备

### 1. 先执行 SQL 脚本

在 PostgreSQL 中执行 `V2_user_role_enhancement.sql`（即你已经看到的那个文件），它会：
- 创建 `sys_user_role` 关联表
- 向 `sys_role` 插入四种标准角色（SUPER_ADMIN / FOLDER_ADMIN / DEPT_MANAGER / EMPLOYEE）
- 迁移旧的 `sys_user.role_id` 数据到新的 `sys_user_role` 表

### 2. 确保 Redis 启动

Sa-Token 的 Session 依赖 Redis，本地 `localhost:6379` 需要可用。

### 3. 启动应用

应用端口 `8080`，基地址 `http://localhost:8080`。

### 4. Apifox 全局设置

在 Apifox 的环境变量中，设置一个变量 `{{token}}`，用于在后续接口中自动带上 Token。

在请求的 Header 中统一添加：

| Header Key | Value |
|---|---|
| `Authorization` | `{{token}}` |

---

## 测试流程（共 16 步，按顺序执行）

---

### 场景一：基础登录链路验证（体现 roleCodes 改造）

#### Step 1 — 注册一个测试用户（无需 Token）

```
POST http://localhost:8080/api/v1/auth/register
```

Body（JSON）：

```json
{
  "username": "testadmin",
  "password": "12345678",
  "realName": "测试管理员",
  "email": "admin@test.com",
  "mobile": "13800000001",
  "deptId": 1,
  "jobLevel": 1
}
```

**预期响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userId": 1234567890,
    "status": "PENDING"
  }
}
```

**注意：** 注册后用户 `status=0`（PENDING），你需要先去数据库手动改为 `1`（启用）：

```sql
UPDATE sys_user SET status = 1 WHERE username = 'testadmin';
```

#### Step 2 — 登录（体现返回 roleCodes 的改造）

```
POST http://localhost:8080/api/v1/auth/login
```

Body（JSON）：

```json
{
  "username": "testadmin",
  "password": "12345678"
}
```

**预期响应（改造后的新结构）：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "token": "xxxxx-xxxxx-xxxxx-xxxxx",
    "expireIn": 2592000,
    "user": {
      "id": 1234567890,
      "username": "testadmin",
      "realName": "测试管理员",
      "email": "admin@test.com",
      "mobile": "13800000001",
      "deptId": 1,
      "jobLevel": 1,
      "status": 1,
      "roleCodes": []
    },
    "roleCodes": []
  }
}
```

**你应该关注的改造点：**
- 顶层多了 `roleCodes` 字段（新增）
- `user` 对象内多了 `roleCodes` 和 `deptId` 字段（新增）
- 此时 `roleCodes` 为空数组 `[]`，因为还没有给这个用户分配角色

**操作：** 把返回的 `token` 值复制到 Apifox 环境变量 `{{token}}` 中（或用 Apifox 的后置脚本自动提取）。

Apifox 后置脚本（可选，自动提取 Token）：

```javascript
var res = pm.response.json();
if (res.code === 0 && res.data && res.data.token) {
    pm.environment.set("token", res.data.token);
}
```

#### Step 3 — 获取当前用户信息（新增接口 /me）

```
GET http://localhost:8080/api/v1/auth/me
Header: Authorization = {{token}}
```

**预期响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1234567890,
    "username": "testadmin",
    "realName": "测试管理员",
    "email": "admin@test.com",
    "mobile": "13800000001",
    "deptId": 1,
    "jobLevel": 1,
    "status": 1,
    "roleCodes": []
  }
}
```

**验证点：** `/api/v1/auth/me` 是本次新增的接口，能正常返回当前登录用户信息和角色码列表。

---

### 场景二：无角色时的权限拦截验证

#### Step 4 — 无角色用户访问用户管理接口（应被拦截）

```
GET http://localhost:8080/api/v1/users/1
Header: Authorization = {{token}}
```

**预期响应：**

```json
{
  "code": 4031001,
  "message": "权限不足"
}
```

**验证点：** 当前用户没有任何角色，`AuthInterceptor` 检测到没有 `SUPER_ADMIN` 角色，返回 403。

#### Step 5 — 无角色用户访问部门列表（应被拦截）

```
GET http://localhost:8080/api/v1/departments
Header: Authorization = {{token}}
```

**预期响应：**

```json
{
  "code": 4031001,
  "message": "权限不足"
}
```

**验证点：** 部门管理接口需要 `SUPER_ADMIN` 或 `DEPT_MANAGER` 角色。

#### Step 6 — 无角色用户尝试分配角色（应被拦截）

```
POST http://localhost:8080/api/v1/roles/assign
Header: Authorization = {{token}}
```

Body：

```json
{
  "userId": 1234567890,
  "roleCode": "SUPER_ADMIN",
  "isPrimary": true
}
```

**预期响应：**

```json
{
  "code": 4031001,
  "message": "角色权限不足"
}
```

**验证点：** `@SaCheckRole("SUPER_ADMIN")` 注解生效，`NotRoleException` 被 `GlobalExceptionHandler` 捕获并返回友好错误信息（不是 500）。

---

### 场景三：手动给用户分配角色后再测试

由于当前没有 SUPER_ADMIN 用户能调接口分配角色，你需要**直接在数据库插入一条角色关联记录**：

```sql
-- 假设 testadmin 的 user id 是 1234567890（替换为实际ID）
INSERT INTO sys_user_role (user_id, role_code, is_primary, status, source_type, deleted, created_by, updated_by)
VALUES (你的实际userId, 'SUPER_ADMIN', true, 1, 2, false, 'system', 'system');
```

#### Step 7 — 重新登录（刷新角色缓存）

```
POST http://localhost:8080/api/v1/auth/login
```

Body 同 Step 2。

**预期响应：**

```json
{
  "code": 0,
  "data": {
    "token": "new-token-value",
    "expireIn": 2592000,
    "user": {
      "id": 1234567890,
      "username": "testadmin",
      "roleCodes": ["SUPER_ADMIN"]
    },
    "roleCodes": ["SUPER_ADMIN"]
  }
}
```

**验证点：** `roleCodes` 现在不再为空，包含 `["SUPER_ADMIN"]`。这证明登录流程改造后，角色信息从 `sys_user_role` 表查询并写入了 Session。

**操作：** 更新 `{{token}}` 为新 Token。

#### Step 8 — SUPER_ADMIN 访问用户管理（应放行）

```
GET http://localhost:8080/api/v1/users/你的userId
Header: Authorization = {{token}}
```

**预期响应：**

```json
{
  "code": 0,
  "data": {
    "id": 1234567890,
    "username": "testadmin",
    "roleCodes": ["SUPER_ADMIN"]
  }
}
```

**验证点：** 
- 有 SUPER_ADMIN 角色后，`AuthInterceptor` 放行
- `UserServiceImpl.getById()` 改造后也返回了 `roleCodes`

---

### 场景四：角色分配/撤销接口（核心新增功能）

#### Step 9 — 先注册第二个用户

```
POST http://localhost:8080/api/v1/auth/register
```

```json
{
  "username": "employee01",
  "password": "12345678",
  "realName": "普通员工小王",
  "email": "wang@test.com",
  "mobile": "13800000002",
  "deptId": 1,
  "jobLevel": 3
}
```

注册后在数据库中启用：

```sql
UPDATE sys_user SET status = 1 WHERE username = 'employee01';
```

#### Step 10 — 用 SUPER_ADMIN 的 Token 给第二个用户分配角色

```
POST http://localhost:8080/api/v1/roles/assign
Header: Authorization = {{token}}  (testadmin 的 SUPER_ADMIN Token)
```

Body（用 employee01 的实际 userId）：

```json
{
  "userId": employee01的ID,
  "roleCode": "EMPLOYEE",
  "isPrimary": true
}
```

**预期响应：**

```json
{
  "code": 0,
  "message": "success"
}
```

**验证点：** SUPER_ADMIN 可以成功分配角色。

#### Step 11 — 重复分配同一角色（应被拦截）

再次发送 Step 10 的请求。

**预期响应：**

```json
{
  "code": 4001002,
  "message": "资源冲突"
}
```

**验证点：** 部分唯一索引 `uk_user_role_active` 生效，Service 层提前检查并拦截。

#### Step 12 — 给同一用户再加一个角色（一人多角色）

```
POST http://localhost:8080/api/v1/roles/assign
Header: Authorization = {{token}}
```

```json
{
  "userId": employee01的ID,
  "roleCode": "DEPT_MANAGER",
  "isPrimary": false
}
```

**预期响应：** `code: 0`

#### Step 13 — 查询用户的角色列表

```
GET http://localhost:8080/api/v1/roles/user/employee01的ID
Header: Authorization = {{token}}
```

**预期响应：**

```json
{
  "code": 0,
  "data": [
    {
      "id": 111,
      "userId": xxx,
      "roleCode": "EMPLOYEE",
      "roleName": "普通员工",
      "isPrimary": true,
      "status": 1,
      "sourceType": 1
    },
    {
      "id": 222,
      "userId": xxx,
      "roleCode": "DEPT_MANAGER",
      "roleName": "部门经理",
      "isPrimary": false,
      "status": 1,
      "sourceType": 1
    }
  ]
}
```

**验证点：** 一人多角色，主角色标记正确，角色名称从枚举自动映射。

#### Step 14 — 撤销角色

```
DELETE http://localhost:8080/api/v1/roles/revoke?userId=employee01的ID&roleCode=DEPT_MANAGER
Header: Authorization = {{token}}
```

**预期响应：** `code: 0`

再次查询 Step 13，应只剩 `EMPLOYEE` 一个角色。

---

### 场景五：角色变更后缓存实时刷新

#### Step 15 — 验证不重新登录角色即时生效

1. 先用 `employee01` 登录，拿到他的 Token（此时他有 EMPLOYEE 角色）
2. 用 `testadmin`（SUPER_ADMIN）的 Token，给 `employee01` 追加 `FOLDER_ADMIN` 角色：

```
POST http://localhost:8080/api/v1/roles/assign
```

```json
{
  "userId": employee01的ID,
  "roleCode": "FOLDER_ADMIN"
}
```

3. **不重新登录 employee01**，直接用 employee01 的旧 Token 调：

```
GET http://localhost:8080/api/v1/auth/me
Header: Authorization = employee01的Token
```

**预期响应：**

```json
{
  "data": {
    "roleCodes": ["EMPLOYEE", "FOLDER_ADMIN"]
  }
}
```

**验证点：** `assignRole()` 内部调用了 `refreshUserRoleSession()`，直接更新了 employee01 在 Redis 中的 Session，**无需重新登录**即可生效。

---

### 场景六：退出登录

#### Step 16 — 退出登录后 Token 失效

```
POST http://localhost:8080/api/v1/auth/logout
Header: Authorization = {{token}}
```

**预期响应：** `code: 0`

然后再用同一个 Token 调任何接口：

```
GET http://localhost:8080/api/v1/auth/me
Header: Authorization = {{token}}  (已失效的Token)
```

**预期响应：**

```json
{
  "code": 4011001,
  "message": "请先登录"
}
```

---

## 总结：每个改造点对应的验证步骤

| 改造点 | 对应测试步骤 |
|---|---|
| 登录返回 `roleCodes` | Step 2, Step 7 |
| 新增 `/me` 接口 | Step 3 |
| 新增 `/logout` 接口 | Step 16 |
| 无角色被权限拦截 | Step 4, 5, 6 |
| `AuthInterceptor` 路径分级鉴权 | Step 4（users需SUPER_ADMIN）, Step 5（departments需SUPER_ADMIN/DEPT_MANAGER） |
| `@SaCheckRole` 注解生效 | Step 6 |
| `GlobalExceptionHandler` 捕获 Sa-Token 异常 | Step 6（返回4031001而非500） |
| SUPER_ADMIN 放行管理接口 | Step 8 |
| `UserDetailRespDTO` 增加 `roleCodes` + `deptId` | Step 3, Step 8 |
| 角色分配接口 | Step 10 |
| 重复绑定拦截 | Step 11 |
| 一人多角色 | Step 12, Step 13 |
| 角色撤销 | Step 14 |
| 角色变更后 Session 实时刷新 | Step 15 |
| Token 失效 | Step 16 |

建议在 Apifox 中建一个目录叫 **"Auth 角色改造验证"**，按上面 16 个步骤创建接口用例，可以一键跑完整个流程。